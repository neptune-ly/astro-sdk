import{_ as t,o as a,c as i,ag as s}from"./chunks/framework.DFmy_ALI.js";const u=JSON.parse('{"title":"Direct Merchant Integration","description":"","frontmatter":{},"headers":[],"relativePath":"guide/direct-integration.md","filePath":"guide/direct-integration.md"}'),o={name:"guide/direct-integration.md"};function n(r,e,l,h,d,c){return a(),i("div",null,[...e[0]||(e[0]=[s(`<h1 id="direct-merchant-integration" tabindex="-1">Direct Merchant Integration <a class="header-anchor" href="#direct-merchant-integration" aria-label="Permalink to &quot;Direct Merchant Integration&quot;">​</a></h1><p>Astro supports direct merchant integration without a Neptune SDK. This path is for merchants that already have a backend team, want to call the REST APIs directly, and are approved for direct API access by operations.</p><p>Astro supports two direct-integration models:</p><ol><li>Standard direct API plus OpenWave-hosted checkout</li><li>Trusted merchant-hosted checkout for explicitly approved merchants</li></ol><p>If you are Ezone or another merchant already approved for trusted merchant-hosted checkout, start with <a href="./ezone-quickstart.html">Ezone Quick Start</a>. That page gives the exact call sequence without SDK-oriented background material.</p><p>The default model used by most merchants, including the original Ezone setup, is:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Merchant backend -&gt; Astro API -&gt; hosted checkout -&gt; signed webhook -&gt; merchant order system</span></span></code></pre></div><p>For a trusted merchant that is approved to keep the customer on the merchant site or app, Astro can also expose a separate merchant-hosted checkout API surface:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Merchant frontend -&gt; merchant backend -&gt; Astro merchant-hosted checkout APIs -&gt; bank authorization via Astro orchestration -&gt; signed webhook -&gt; merchant order system</span></span></code></pre></div><p>The security rule does not change:</p><ul><li>merchant keys stay on the merchant backend</li><li>customers still complete authorization through Astro-owned bank orchestration</li><li>fulfilment waits for a signed final webhook such as <code>payment.completed</code></li></ul><h2 id="direct-api-vs-sdk" tabindex="-1">Direct API vs SDK <a class="header-anchor" href="#direct-api-vs-sdk" aria-label="Permalink to &quot;Direct API vs SDK&quot;">​</a></h2><p>Direct API access and SDK access are related but not equivalent:</p><ul><li><code>Direct API</code> means your backend can create sessions, query status, register webhook endpoints, and perform other server-to-server operations with a merchant key.</li><li><code>SDK</code> means you may also use the official client or mobile helpers where Astro exposes them.</li><li>the standard direct API mode does not let the merchant replace hosted checkout with a merchant-owned bank authorization page</li><li>the trusted merchant-hosted mode is a separate approval and must be enabled explicitly per merchant</li><li>the merchant backend is the only supported caller to Astro for this mode; do not expose merchant API keys in browser or mobile client code</li><li>customer bank authentication still runs through Astro-orchestrated authorization, even when the customer remains inside the trusted merchant experience</li></ul><p>Choose direct API when your team wants full server-side control and does not need client-side SDK helpers. Choose an SDK when you want maintained browser or mobile components, built-in webhook helpers, or a faster path for app-side integration.</p><h2 id="who-should-use-this-path" tabindex="-1">Who should use this path <a class="header-anchor" href="#who-should-use-this-path" aria-label="Permalink to &quot;Who should use this path&quot;">​</a></h2><p>Use direct integration when all of the following are true:</p><ul><li>the merchant has direct API access enabled</li><li>the merchant can safely store bearer keys on a backend service</li><li>the merchant can receive HTTPS webhooks</li><li>the merchant wants to call Astro over REST instead of using a maintained SDK</li></ul><p>If the merchant needs browser components, mobile helpers, or a faster first integration, use one of the official SDKs instead.</p><h2 id="trusted-merchant-hosted-checkout" tabindex="-1">Trusted merchant-hosted checkout <a class="header-anchor" href="#trusted-merchant-hosted-checkout" aria-label="Permalink to &quot;Trusted merchant-hosted checkout&quot;">​</a></h2><p>This capability is for a small set of trusted merchants only. It means the customer completes the payment flow inside the merchant website or app instead of being redirected to the OpenWave-hosted checkout page. The merchant does not open an OpenWave page in a redirect, popup, iframe, or embedded webview for this path.</p><p>For the compact endpoint-by-endpoint contract, see <a href="./trusted-merchant-hosted.html">Trusted Merchant-Hosted Checkout</a>.</p><p>Use this model only when all of the following are true:</p><ul><li>Astro operations explicitly enabled merchant-hosted checkout for the merchant</li><li>the merchant already has direct API access and live approval where required</li><li>live merchants have at least one active managed merchant webhook endpoint for recovery and audit</li><li>the merchant can safely collect payer identifiers and bank step-up inputs inside its own PCI-like controlled experience</li><li>the merchant can implement stricter logging, masking, anti-fraud, and replay controls than a normal redirect integration</li></ul><p>Important security boundaries:</p><ul><li>merchant-hosted checkout is not a public replacement for hosted checkout</li><li>it must stay disabled for normal merchants</li><li>customer bank authorization is still validated by Astro and the bank path Astro orchestrates</li><li>final fulfilment still waits for the signed final webhook such as <code>payment.completed</code></li></ul><p>Trusted merchant-hosted endpoint family:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>/api/v1/payments/sessions/{session_id}/merchant-checkout/*</span></span></code></pre></div><p>Current flow:</p><ol><li>Merchant backend creates the payment session with the normal merchant key and <code>checkout_surface=TRUSTED_MERCHANT_HOSTED_CHECKOUT</code>. In Neptune Astro, this trusted session-create call must also include <code>Idempotency-Key</code>.</li><li>Astro returns the trusted <code>session_id</code> plus the <code>/merchant-checkout/*</code> API root for that session.</li><li>The merchant backend calls <code>POST /api/v1/payments/sessions/{session_id}/merchant-checkout/access</code> with the long-lived merchant key to mint a fresh short-lived <code>merchant_checkout_session_token</code> for that page load.</li><li>The merchant frontend can call the merchant-hosted checkout endpoints directly with <code>X-Session-Token</code> while the customer stays on the merchant site.</li><li>Astro accepts those browser calls only for trusted merchant-hosted sessions and only from merchant origins approved in Astro. Origin approval is enforced at the HTTP layer as well as inside the checkout authorization logic, so a merchant domain that is not explicitly approved will fail before checkout actions run.</li><li>Merchant backend or frontend resolves payer input through the merchant-hosted checkout endpoints.</li><li>Merchant frontend drives phone verification or typed payer selection where needed.</li><li>Merchant frontend selects the bank authorization method exposed for that session.</li><li>Merchant frontend submits the customer confirmation step through the trusted merchant-hosted confirm path.</li><li>Merchant backend waits for signed webhook completion before fulfilment.</li></ol><p>This model keeps the customer inside the merchant surface while preserving Astro ownership of session state, payer normalization, authorization state, and final payment outcome. The default remains hosted checkout even for a trusted merchant. Merchant-hosted checkout is used only when the merchant explicitly requests the trusted surface during session creation.</p><h3 id="trusted-merchant-hosted-endpoint-inventory" tabindex="-1">Trusted merchant-hosted endpoint inventory <a class="header-anchor" href="#trusted-merchant-hosted-endpoint-inventory" aria-label="Permalink to &quot;Trusted merchant-hosted endpoint inventory&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Method</th><th>Path</th><th>Purpose</th></tr></thead><tbody><tr><td><code>POST</code></td><td><code>/api/v1/payments/sessions/{session_id}/merchant-checkout/access</code></td><td>Backend-only exchange from merchant bearer key to short-lived <code>X-Session-Token</code>.</td></tr><tr><td><code>POST</code></td><td><code>/api/v1/payments/sessions/{session_id}/merchant-checkout/resolve-payer</code></td><td>Resolve alias or IBAN payer route and return bank/auth options.</td></tr><tr><td><code>POST</code></td><td><code>/api/v1/payments/sessions/{session_id}/merchant-checkout/phone/start</code></td><td>Start phone OTP verification for phone-based payer choice.</td></tr><tr><td><code>POST</code></td><td><code>/api/v1/payments/sessions/{session_id}/merchant-checkout/phone/verify</code></td><td>Verify phone OTP and return masked linked account choices.</td></tr><tr><td><code>POST</code></td><td><code>/api/v1/payments/sessions/{session_id}/merchant-checkout/phone/select</code></td><td>Select one masked phone-linked account and bind it to the payment session.</td></tr><tr><td><code>POST</code></td><td><code>/api/v1/payments/sessions/{session_id}/merchant-checkout/select-auth</code></td><td>Trigger the bank authorization method for the resolved payer.</td></tr><tr><td><code>POST</code></td><td><code>/api/v1/payments/sessions/{session_id}/merchant-checkout/confirm</code></td><td>Submit bank OTP for verification and payment execution.</td></tr><tr><td><code>GET</code></td><td><code>/api/v1/payments/sessions/{session_id}/merchant-checkout/push-status</code></td><td>Poll push approval state and payment status.</td></tr><tr><td><code>GET</code></td><td><code>/api/v1/payments/sessions/{session_id}/merchant-checkout/status</code></td><td>Read the trusted checkout payment state plus the next recommended Astro step.</td></tr></tbody></table><h3 id="trusted-merchant-hosted-auth-model" tabindex="-1">Trusted merchant-hosted auth model <a class="header-anchor" href="#trusted-merchant-hosted-auth-model" aria-label="Permalink to &quot;Trusted merchant-hosted auth model&quot;">​</a></h3><ul><li>Merchant backend calls <code>POST /merchant-checkout/access</code> with <code>Authorization: Bearer mk_test_...</code> or <code>mk_live_...</code>.</li><li>Astro returns <code>merchant_checkout_session_token</code>.</li><li>Merchant frontend or backend proxy continues the flow with <code>X-Session-Token: &lt;merchant_checkout_session_token&gt;</code>.</li><li>The long-lived merchant bearer key stays backend-only.</li><li>The session token is short-lived, scoped to one payment session, and valid only for trusted merchant-hosted checkout.</li><li>Browser calls with <code>X-Session-Token</code> must come from an origin explicitly approved on the merchant profile.</li></ul><h3 id="initiate-contract-for-trusted-merchant-hosted-checkout" tabindex="-1">Initiate contract for trusted merchant-hosted checkout <a class="header-anchor" href="#initiate-contract-for-trusted-merchant-hosted-checkout" aria-label="Permalink to &quot;Initiate contract for trusted merchant-hosted checkout&quot;">​</a></h3><p>Set:</p><ul><li><code>checkout_surface = &quot;TRUSTED_MERCHANT_HOSTED_CHECKOUT&quot;</code></li><li><code>Idempotency-Key: &lt;stable value&gt;</code></li></ul><p>Live-only gates:</p><ul><li>direct API access enabled</li><li>merchant-hosted checkout enabled</li><li>live enabled</li><li>at least one active managed merchant webhook endpoint</li></ul><h3 id="state-machine" tabindex="-1">State machine <a class="header-anchor" href="#state-machine" aria-label="Permalink to &quot;State machine&quot;">​</a></h3><ol><li><code>PENDING</code><ul><li>Resolve payer with alias, IBAN, or verified phone flow.</li></ul></li><li><code>OTP_SENT</code><ul><li>Customer received bank OTP.</li><li><code>next_action = SUBMIT_OTP</code></li></ul></li><li><code>PUSH_SENT</code><ul><li>Customer received bank push challenge.</li><li><code>next_action = POLL_PUSH_STATUS</code></li></ul></li><li><code>EXECUTING</code><ul><li>Bank challenge passed and Astro is executing the payment.</li><li><code>next_action = POLL_PAYMENT_STATUS</code></li></ul></li><li>Terminal status: <ul><li><code>COMPLETED</code></li><li><code>FAILED</code></li><li><code>EXPIRED</code></li><li><code>CANCELLED</code></li><li><code>RECONCILIATION_REQUIRED</code></li></ul></li></ol><p>Support-safe external status values remain the same as the standard merchant status API.</p><h3 id="otp-and-push-specifics" tabindex="-1">OTP and push specifics <a class="header-anchor" href="#otp-and-push-specifics" aria-label="Permalink to &quot;OTP and push specifics&quot;">​</a></h3><ul><li>Phone verification OTP: <ul><li>Astro challenge TTL is 5 minutes.</li><li>Max failed verification attempts per challenge: 5.</li><li>Resend ceiling per session window: 3 sends.</li><li>Session/IP throttling is enforced before new challenges are created.</li></ul></li><li>Bank payment OTP: <ul><li>Merchant triggers or retriggers it through <code>POST /select-auth</code> with <code>auth_mode = OTP</code>.</li><li>Astro returns <code>otp_expires_in_seconds</code> when the bank reports it.</li><li>Invalid OTP returns <code>INVALID_OTP</code>.</li><li>Expired session returns <code>SESSION_EXPIRED</code>.</li></ul></li><li>Push: <ul><li>Merchant triggers it through <code>POST /select-auth</code> with <code>auth_mode = PUSH</code>.</li><li>Poll <code>GET /push-status</code> every 2 to 5 seconds.</li><li>Terminal push states are <code>APPROVED</code>, <code>REJECTED</code>, or <code>EXPIRED</code>.</li></ul></li></ul><h3 id="error-model-and-idempotency" tabindex="-1">Error model and idempotency <a class="header-anchor" href="#error-model-and-idempotency" aria-label="Permalink to &quot;Error model and idempotency&quot;">​</a></h3><ul><li>Merchant-safe error envelope: <ul><li><code>code</code></li><li><code>message</code></li><li><code>request_id</code></li><li>optional <code>details</code></li></ul></li><li>Important codes in this flow include: <ul><li><code>INVALID_OTP</code></li><li><code>SESSION_EXPIRED</code></li><li><code>SESSION_INVALID_STATE</code></li><li><code>PAYER_RESOLUTION_FAILED</code></li><li><code>IDEMPOTENCY_CONFLICT</code></li><li>bank-safe payment rejection codes such as <code>INSUFFICIENT_FUNDS</code></li></ul></li><li><code>Idempotency-Key</code> is mandatory for trusted merchant-hosted session creation and recommended for all merchant write calls.</li></ul><h3 id="completion-and-webhooks" tabindex="-1">Completion and webhooks <a class="header-anchor" href="#completion-and-webhooks" aria-label="Permalink to &quot;Completion and webhooks&quot;">​</a></h3><ul><li><code>payment.completed</code> remains the authoritative completion event for trusted merchant-hosted checkout exactly as it does for hosted checkout.</li><li>Browser status and redirect-style signals are not the final fulfilment control.</li><li>Merchant systems should still verify and durably record the signed webhook before fulfilment.</li></ul><h3 id="sandbox-fixtures" tabindex="-1">Sandbox fixtures <a class="header-anchor" href="#sandbox-fixtures" aria-label="Permalink to &quot;Sandbox fixtures&quot;">​</a></h3><p>Use the current Astro sandbox with:</p><ul><li>approved sandbox merchant key</li><li>a valid sandbox alias or IBAN already provisioned in the connected test bank/identity environment</li><li>the standard sandbox webhook receiver path</li></ul><p>The exact current sandbox customer fixtures and any bank-specific test OTP are bank-environment controlled and should be handed off by Neptune operations with the merchant enablement note, not hardcoded into public docs.</p><h3 id="compliance-and-browser-server-split" tabindex="-1">Compliance and browser/server split <a class="header-anchor" href="#compliance-and-browser-server-split" aria-label="Permalink to &quot;Compliance and browser/server split&quot;">​</a></h3><ul><li><code>POST /merchant-checkout/access</code> is strictly backend-only.</li><li>All other <code>/merchant-checkout/*</code> calls may run either from the merchant backend proxy or directly from the approved merchant frontend with <code>X-Session-Token</code>.</li><li>Trusted merchants may collect payer identifiers and bank OTP on their own surface for this lane only.</li><li>Merchants must not store raw OTP values after submission, replay bank challenges outside Astro, or downgrade final fulfilment from signed webhook confirmation to browser-only state.</li></ul><h2 id="environment-model" tabindex="-1">Environment model <a class="header-anchor" href="#environment-model" aria-label="Permalink to &quot;Environment model&quot;">​</a></h2><p>Astro separates sandbox and live by credential and operator enablement. There is no request flag that turns a sandbox key into a live request.</p><table tabindex="0"><thead><tr><th>Environment</th><th>Typical key prefix</th><th>Intended use</th></tr></thead><tbody><tr><td>Sandbox</td><td><code>mk_test_...</code></td><td>Integration build-out, test webhooks, non-production routing</td></tr><tr><td>Live</td><td><code>mk_live_...</code></td><td>Real customer traffic after readiness review</td></tr></tbody></table><p>Rules:</p><ul><li>sandbox and live are separate credentials with separate key rotation and separate webhook secrets</li><li>the credential prefix is authoritative for environment selection</li><li>a merchant may have sandbox enabled before live is approved</li><li>live traffic is enabled by Astro operations, not by a client-side toggle</li><li>merchants should use separate webhook endpoints, labels, logs, and recovery workflows for sandbox and live where possible</li></ul><p>For operator onboarding, live enablement should require:</p><ul><li>readiness review completed</li><li>default settlement account approved and active</li><li>at least one successful webhook test</li><li>support contact confirmed</li><li>merchant informed which live key or channel key to use</li></ul><h2 id="minimal-direct-payment-flow" tabindex="-1">Minimal direct payment flow <a class="header-anchor" href="#minimal-direct-payment-flow" aria-label="Permalink to &quot;Minimal direct payment flow&quot;">​</a></h2><h3 id="_1-create-the-session-from-your-backend" tabindex="-1">1. Create the session from your backend <a class="header-anchor" href="#_1-create-the-session-from-your-backend" aria-label="Permalink to &quot;1. Create the session from your backend&quot;">​</a></h3><div class="language-http vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">http</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">POST</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> /api/v1/payments/initiate</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">Authorization</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> Bearer mk_test_...</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">Content-Type</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> application/json</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">Idempotency-Key</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> order_1042_create_session</span></span></code></pre></div><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;amount&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">860000</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;currency&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;LYD&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;merchant_reference&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;EZONE-1042&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;description&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Ezone order 1042&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;destination&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;type&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;alias&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;value&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;merchant@andalus&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  },</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;redirect_url&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;https://merchant.example.com/orders/EZONE-1042&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;webhook_url&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;https://merchant.example.com/openwave/webhooks&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>When resolving an alias later in the flow, use a real enrolled NPT handle from the target environment, for example <code>customer123@andalus</code>. Do not use docs placeholders such as <code>tellesy@andalus</code> as if they were production fixtures.</p><p>The response returns <code>payment_url</code>, <code>session_id</code>, <code>checkout_surface</code>, <code>authorized_checkout_surfaces</code>, <code>next_action</code>, and, for trusted sessions only, <code>merchant_checkout_api_root</code>.</p><ul><li>Standard direct integration: redirect the customer to the returned hosted page.</li><li>Trusted merchant-hosted integration: keep the session id and use the approved <code>/merchant-checkout/*</code> endpoints instead of redirecting to <code>payment_url</code>.</li><li>Even in trusted merchant-hosted mode, the customer stays on the merchant UX and the merchant frontend may call Astro directly only after the merchant backend exchanges the session into a short-lived <code>merchant_checkout_session_token</code>. Do not expose the long-lived merchant API key in browser or mobile client code.</li><li>Trusted merchant-hosted session creation is rejected if <code>Idempotency-Key</code> is missing.</li></ul><h3 id="_2-continue-the-customer-flow-on-the-approved-surface" tabindex="-1">2. Continue the customer flow on the approved surface <a class="header-anchor" href="#_2-continue-the-customer-flow-on-the-approved-surface" aria-label="Permalink to &quot;2. Continue the customer flow on the approved surface&quot;">​</a></h3><p>For the standard direct API model, the merchant backend should return the hosted URL to the frontend and then perform a normal redirect. The Astro checkout page is where the customer:</p><ul><li>reviews the amount and merchant</li><li>chooses or confirms the payer route</li><li>authenticates with the bank</li><li>returns to the merchant <code>redirect_url</code> after the hosted step</li></ul><p>For the trusted merchant-hosted model, the merchant stays inside its own frontend and uses the merchant-hosted checkout endpoints to:</p><ul><li>resolve alias, IBAN, or verified phone payer input</li><li>select the allowed authorization path for the session</li><li>submit the bank authorization step through the trusted Astro API surface</li><li>poll push status where the bank flow is asynchronous</li></ul><p>When the merchant frontend calls Astro directly, it must send:</p><ul><li><code>X-Session-Token: &lt;merchant_checkout_session_token&gt;</code></li><li>the request from a merchant origin that Astro has explicitly allowed for that merchant</li></ul><p>If the merchant wants to keep all Astro traffic server-to-server instead, that remains supported. The short-lived session token path is intended to make the browser experience cleaner without exposing the permanent merchant credential. The trusted browser path is never open-ended: it is gated by merchant approval, session-scoped token, explicit trusted checkout surface, and approved browser origin.</p><h3 id="_3-wait-for-the-signed-webhook" tabindex="-1">3. Wait for the signed webhook <a class="header-anchor" href="#_3-wait-for-the-signed-webhook" aria-label="Permalink to &quot;3. Wait for the signed webhook&quot;">​</a></h3><p>Use the browser redirect only as a customer experience signal. Do not treat it as the final payment confirmation.</p><p>The merchant should fulfil only after:</p><ul><li>the webhook signature is valid</li><li>the event is new or idempotently safe to replay</li><li>the event is a final state such as <code>payment.completed</code></li><li>the event has been durably recorded or queued for reliable downstream processing before returning HTTP <code>2xx</code></li></ul><h2 id="identifier-handling-alias-vs-iban-vs-phone" tabindex="-1">Identifier handling: alias vs IBAN vs phone <a class="header-anchor" href="#identifier-handling-alias-vs-iban-vs-phone" aria-label="Permalink to &quot;Identifier handling: alias vs IBAN vs phone&quot;">​</a></h2><p>When the merchant or customer provides a payer identifier, classify the value by the actual content.</p><table tabindex="0"><thead><tr><th>Input shape</th><th>Treat as</th></tr></thead><tbody><tr><td><code>LY...</code></td><td><code>payer_iban</code></td></tr><tr><td><code>09...</code> or <code>+218...</code></td><td>phone-based payer flow, when enabled</td></tr><tr><td><code>name@bank</code> or <code>name</code></td><td>NPT alias</td></tr></tbody></table><p>For direct integrations:</p><ul><li>do not send a Libyan IBAN through alias-labeled fields</li><li>do not label an IBAN as an NPT tag in callback or UI text</li><li>if the merchant UI uses a generic input, normalize it server-side before calling Astro</li></ul><p>Recommended examples:</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;payer_alias&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;tellesy@andalus&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;payer_iban&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;LY8302700000000000000000001&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>Do not rely on free-form customer input reaching the API unchanged. Merchant systems should normalize and validate the identifier before submitting the request and should keep audit logs that show which typed field was used.</p><h2 id="webhook-setup" tabindex="-1">Webhook setup <a class="header-anchor" href="#webhook-setup" aria-label="Permalink to &quot;Webhook setup&quot;">​</a></h2><p>Direct API merchants do not need an SDK to receive webhooks. They do need:</p><ul><li>an HTTPS endpoint</li><li>raw-body signature verification</li><li>idempotent processing</li><li>delivery log recovery</li><li>separate operational handling for sandbox and live</li></ul><p>Recommended endpoint management flow:</p><div class="language-http vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">http</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">POST</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> /api/v1/merchant/webhook-endpoints</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">Authorization</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> Bearer mk_test_...</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">Content-Type</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> application/json</span></span></code></pre></div><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;label&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Ezone sandbox orders&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;url&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;https://merchant.example.com/openwave/webhooks&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;event_types&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;payment.*&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;refund.*&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;mandate.*&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>The signing secret should be stored server-side and revealed only once at creation or rotation time.</p><p>Prefer managed webhook endpoints over the older single <code>webhook_url</code> compatibility field. Managed endpoints support event subscriptions, secret rotation, endpoint-specific logs, and clearer recovery. The request-level <code>webhook_url</code> on <code>POST /api/v1/payments/initiate</code> is only a per-session override; it does not replace your saved managed endpoint configuration.</p><p>Webhook receiver expectations:</p><ul><li>verify <code>X-OpenWave-Signature</code> against the raw request body</li><li>use constant-time comparison</li><li>store <code>event_id</code> or equivalent idempotency marker before fulfilment</li><li>return HTTP <code>2xx</code> only after the event is durably recorded</li><li>expect retries with backoff when your receiver is unavailable or returns non-<code>2xx</code></li></ul><h2 id="webhook-recovery-and-reports" tabindex="-1">Webhook recovery and reports <a class="header-anchor" href="#webhook-recovery-and-reports" aria-label="Permalink to &quot;Webhook recovery and reports&quot;">​</a></h2><p>If the receiver is unavailable or the merchant loses an event, recovery should happen through the webhook delivery log and reports, not by asking operations to resend blind copies.</p><p>Useful API paths:</p><div class="language-http vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">http</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">GET</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> /api/v1/merchant/webhook-endpoints</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">GET</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> /api/v1/webhooks?status=FAILED&amp;limit=50</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">GET</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> /api/v1/webhooks/health</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">GET</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> /api/v1/merchant/reports/payments?format=csv</span></span></code></pre></div><p>Recommended merchant recovery workflow:</p><ol><li>Inspect endpoint health and failed deliveries.</li><li>Filter by date, event type, account, or checkout channel.</li><li>Recover the payload details needed by the receiver.</li><li>Retry or replay safely inside the merchant system.</li></ol><h2 id="sandbox-to-live-handoff" tabindex="-1">Sandbox to live handoff <a class="header-anchor" href="#sandbox-to-live-handoff" aria-label="Permalink to &quot;Sandbox to live handoff&quot;">​</a></h2><p>The handoff to live should be explicit.</p><p>Recommended operator checklist:</p><ol><li>Confirm the merchant can create sessions in sandbox.</li><li>Confirm hosted checkout redirects correctly.</li><li>Confirm a signed sandbox webhook is received, raw-body verified, and processed idempotently.</li><li>Confirm the merchant understands the difference between redirect, non-final status, and final webhook state.</li><li>Confirm the merchant has separate sandbox and live webhook URLs or an equivalent environment split.</li><li>Enable live access for the merchant or selected live checkout channels.</li><li>Deliver the live key or live channel key through the approved operator path.</li></ol><p>Recommended merchant checklist:</p><ol><li>Replace <code>mk_test_...</code> with the approved <code>mk_live_...</code> or live channel key.</li><li>Point live traffic to the live webhook endpoint and live webhook secret.</li><li>Keep sandbox and live logs, secrets, and recovery procedures separate.</li><li>Re-run a live test if operations policy allows it.</li><li>Confirm fulfilment still waits for <code>payment.completed</code>, not only for redirect return.</li></ol><h2 id="testing-expectations" tabindex="-1">Testing expectations <a class="header-anchor" href="#testing-expectations" aria-label="Permalink to &quot;Testing expectations&quot;">​</a></h2><p>Before asking for live enablement, the merchant should be able to show:</p><ol><li>a sandbox session created with backend-only credentials</li><li>a customer redirect into hosted checkout using the returned <code>payment_url</code></li><li>a valid <code>X-OpenWave-Signature</code> verification against the raw webhook body</li><li>idempotent handling of duplicate deliveries</li><li>status lookup or report-based recovery for a missed webhook</li><li>internal logs that correlate merchant order reference, Astro session id, and webhook event id</li></ol><h2 id="ezone-style-onboarding-wording" tabindex="-1">Ezone-style onboarding wording <a class="header-anchor" href="#ezone-style-onboarding-wording" aria-label="Permalink to &quot;Ezone-style onboarding wording&quot;">​</a></h2><p>For merchants like Ezone, the operator message should be unambiguous:</p><ul><li>no external SDK is required</li><li>direct API access is for backend calls; SDK access is optional and separate</li><li>the standard path uses Astro hosted checkout</li><li>the trusted merchant-hosted path keeps the customer fully on the merchant site or app and is available only for explicitly approved merchants</li><li>webhook integration is required for reliable automation and recommended even when polling exists</li><li>sandbox and live keys, secrets, and endpoints stay separate</li><li>live access is available only after Astro operations enable it</li></ul><p>That wording avoids implying that every merchant must build its own checkout SDK or bank-auth surface, while still making the trusted merchant-hosted lane clear for merchants that Neptune explicitly approves.</p><h2 id="see-also" tabindex="-1">See also <a class="header-anchor" href="#see-also" aria-label="Permalink to &quot;See also&quot;">​</a></h2><ul><li><a href="./quickstart.html">Quick Start</a></li><li><a href="./webhooks.html">Webhooks</a></li><li><a href="./testing.html">Testing &amp; Sandbox</a></li><li><a href="./merchant-operations.html">Merchant Operations</a></li><li><a href="./operational-readiness.html">Operational Readiness</a></li></ul>`,121)])])}const k=t(o,[["render",n]]);export{u as __pageData,k as default};
