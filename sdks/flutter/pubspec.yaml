name: astro_sdk
description: Astro SDK for OpenWave payments — Neptune Fintech
version: 1.0.0
homepage: https://github.com/Tellesy/neptune-astro/tree/main/packages/astro-flutter

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  crypto: ^3.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
